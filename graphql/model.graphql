scalar JSON

type Marque{
    marque: String
    count: Int
}

type Instrument{
    instrument: String
    count: Int
}


type Query {
    marques(mini: Int): [Marque]
    instruments: [Instrument]
}
